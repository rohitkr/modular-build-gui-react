{
  "name": "build-gui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.16.2",
    "body-parser": "^1.18.0",
    "express": "^4.15.4",
    "fetch": "^1.1.0",
    "mongodb": "^2.2.31",
    "morgan": "^1.8.2",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "^1.0.13"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:server": "node server",
    "build": "react-scripts build",
    "build:start:server": "npm run build && npm run start:server",
    "watch": "nodemon --exec 'npm run build:start:server' --watch src",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
